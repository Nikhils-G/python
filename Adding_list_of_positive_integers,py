def sum_of_positive_integers(numbers):
    total = 0
    for num in numbers:
        if num > 0:
            total += num
    return total

# Test the function
numbers = [1, -2, 3, -4, 5]
print(sum_of_positive_integers(numbers))  # Output: 9
